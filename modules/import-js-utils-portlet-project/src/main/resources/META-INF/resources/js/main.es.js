import {add, multiply} from 'js-utils/util.es';

class main {
    addNumbers(a, b) {
        console.log(add(a, b));


    }

    multiplyNumbers(a, b) {
        console.log(multiply(a, b));
    }
}

export default main;